(() => {
  const ready=()=>{if(!window.sb||!document.getElementById('app'))return setTimeout(ready,250);if(document.getElementById('locationSettings'))return;install()};
  const panel=document.createElement('section');
  function install(){panel.id='locationSettings';panel.className='panel';panel.innerHTML=`<h2>📍 آدرس و نقشه شعب</h2><p class="muted">آدرس و لینک نقشه هر شعبه را از همین پنل مدیریت کن.</p><div class="grid"><div class="field"><label>شعبه مرکز — آدرس</label><input id="loc1Address" value="هرات، ۶۴ متره امام مسلم، ۲۸"></div><div class="field"><label>شعبه مرکز — لینک نقشه</label><input id="loc1Map" placeholder="لینک Google Maps را اینجا بگذار"></div><div class="field"><label>شعبه دوم — آدرس</label><input id="loc2Address" value="هرات، فلکه ۲۹ حمل"></div><div class="field"><label>شعبه دوم — لینک نقشه</label><input id="loc2Map" placeholder="لینک Google Maps را اینجا بگذار"></div></div><button id="saveLocations" class="btn gold" type="button">💾 ذخیره آدرس و نقشه</button><span id="locMsg" class="statusmsg"></span>`;document.getElementById('app').prepend(panel);document.getElementById('saveLocations').onclick=save;load()}
  async function get(key){const r=await sb.from('site_settings').select('value').eq('key',key).maybeSingle();if(r.error)throw r.error;return r.data?.value??''}
  async function put(key,value){const r=await sb.from('site_settings').upsert({key,value,updated_at:new Date().toISOString()},{onConflict:'key'});if(r.error)throw r.error}
  async function load(){try{for(const [id,key] of [['loc1Address','branch1_address'],['loc1Map','branch1_map'],['loc2Address','branch2_address'],['loc2Map','branch2_map']])document.getElementById(id).value=await get(key)||document.getElementById(id).value}catch(e){msg('❌ خطا در خواندن: '+e.message,'#b52b2b')}}
  function msg(t,c){const e=document.getElementById('locMsg');e.textContent=t;e.style.color=c||''}
  async function save(){const b=document.getElementById('saveLocations');b.disabled=true;msg('⏳ در حال ذخیره...');try{await put('branch1_address',document.getElementById('loc1Address').value.trim());await put('branch1_map',document.getElementById('loc1Map').value.trim());await put('branch2_address',document.getElementById('loc2Address').value.trim());await put('branch2_map',document.getElementById('loc2Map').value.trim());msg('✅ آدرس و تنظیمات نقشه ذخیره شد.','#1f7a4d')}catch(e){msg('❌ '+e.message,'#b52b2b')}finally{b.disabled=false}}
  ready();
})();
